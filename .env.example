# Example environment variables for chezmoi testing
# Copy this file to .env and fill in your actual values
# The .env file is in .gitignore and will not be committed
#
# これらの環境変数は chezmoi の設定ファイル (~/.config/chezmoi/chezmoi.toml) を
# 自動生成するために使用されます。これによりプロンプトをスキップできます。

# ============================================================================
# 必須設定 (Required Configuration)
# ============================================================================

# Git configuration for test user
# テストユーザーの Git 設定（必須）
TEST_GIT_NAME=Your Name
TEST_GIT_EMAIL=your.email@example.com

# Chezmoi repository URL for testing
# テスト用の chezmoi リポジトリ URL（設定すると自動的に chezmoi init が実行される）
# If set, chezmoi init will run automatically
CHEZMOI_REPO_URL=https://github.com/yourusername/dotfiles.git

# ============================================================================
# オプション設定 (Optional Configuration)
# ============================================================================

# GitHub Personal Access Token
# GitHub API のレート制限を回避するために使用
# Create a token at: https://github.com/settings/tokens
# Required scopes: public_repo (for public repositories)
GITHUB_TOKEN=

# SSH identity file path (leave empty for default)
# SSH 秘密鍵のパス（空の場合はデフォルト）
TEST_SSH_IDENTITY_FILE=

# Age encryption settings
# Age 暗号化の設定（デフォルト値が設定されているので通常は変更不要）
TEST_AGE_IDENTITY=~/.config/chezmoi/key.txt
TEST_AGE_RECIPIENT=age1lnt3hvd436d02uaen8nelcn6aywephrx7lx6ctxjyeyqwv82x59s8kk7dx
